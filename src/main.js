import Vue from 'vue'
import App from './App.vue'
import json from './manifest.json' // eslint-disable-line no-unused-vars
import icon from './assets/icons/crown.png' // eslint-disable-line no-unused-vars

new Vue({
    el: '#app',
    render: h => h(App)
})
